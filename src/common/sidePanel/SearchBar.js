import React, { Component } from 'react'
import {
    SearchIcon
} from './style'

class SearchBar extends Component {
    
    render() {
        return (
            <SearchIcon />
        )
    }
}

export default SearchBar